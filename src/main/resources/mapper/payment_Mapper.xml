<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.goott.trip.hamster.mapper.paymentMapper">

    <insert id="airplanePay" parameterType="Payment">
        INSERT INTO payment (member_id, order_uuid, air_key, call_first_name, call_last_name, call_country, call_email, call_number,order_time)
        VALUES (#{memberId}, #{orderUuid}, #{airKey}, #{callFirstName}, #{callLastName}, #{callCountry}, #{callEmail}, #{callNumber},#{orderTime})
    </insert>


</mapper>
